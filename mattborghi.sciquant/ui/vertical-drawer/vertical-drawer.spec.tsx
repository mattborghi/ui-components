import React from "react";
import { render } from "@testing-library/react";
import { BasicVerticalDrawer } from "./vertical-drawer.composition";

describe("vertical-drawer", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicVerticalDrawer />);
  //   const rendered = getByText('hello from VerticalDrawer');
  //   expect(rendered).toBeTruthy();
  // });
});
