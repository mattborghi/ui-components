import React from "react";
import { render } from "@testing-library/react";
import { BasicNotificationsPopper } from "./notifications-popper.composition";

describe("notifications-popper", () => {
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicNotificationsPopper />);
  //   const rendered = getByText('hello from NotificationsPopper');
  //   expect(rendered).toBeTruthy();
  // });
  it("dummy", () => {
    true;
  });
});
