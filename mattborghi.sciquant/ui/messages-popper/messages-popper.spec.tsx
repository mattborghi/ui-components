import React from "react";
import { render } from "@testing-library/react";
import { BasicMessagesPopper } from "./messages-popper.composition";

describe("messages-popper", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicMessagesPopper />);
  //   const rendered = getByText('hello from MessagesPopper');
  //   expect(rendered).toBeTruthy();
  // });
});
