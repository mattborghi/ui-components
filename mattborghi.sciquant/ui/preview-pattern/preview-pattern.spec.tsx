import React from "react";
import { render } from "@testing-library/react";
import { BasicPreviewPattern } from "./preview-pattern.composition";

describe("preview-pattern", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicPreviewPattern />);
  //   const rendered = getByText('hello from PreviewPattern');
  //   expect(rendered).toBeTruthy();
  // });
});
