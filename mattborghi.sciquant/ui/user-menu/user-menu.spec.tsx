import React from "react";
import { render } from "@testing-library/react";
import { BasicUserMenu } from "./user-menu.composition";

describe("user-menu", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicUserMenu />);
  //   const rendered = getByText('hello from UserMenu');
  //   expect(rendered).toBeTruthy();
  // });
});
