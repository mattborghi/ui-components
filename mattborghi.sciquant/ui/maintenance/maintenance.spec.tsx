import React from "react";
import { render } from "@testing-library/react";
import { BasicMaintenance } from "./maintenance.composition";

describe("maintenance", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicMaintenance />);
  //   const rendered = getByText('hello from Maintenance');
  //   expect(rendered).toBeTruthy();
  // });
});
