import React from "react";
import { render } from "@testing-library/react";
import { BasicMenuNotifications } from "./menu-notifications.composition";

describe("menu-notifications", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicMenuNotifications />);
  //   const rendered = getByText('hello from MenuNotifications');
  //   expect(rendered).toBeTruthy();
  // });
});
