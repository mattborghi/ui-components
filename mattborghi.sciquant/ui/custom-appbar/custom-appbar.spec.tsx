import React from "react";
import { render } from "@testing-library/react";
import { BasicCustomAppbar } from "./custom-appbar.composition";

describe("custom-appbar", () => {
  it("debug", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicCustomAppbar />);
  //   const rendered = getByText('hello from CustomAppbar');
  //   expect(rendered).toBeTruthy();
  // });
});
