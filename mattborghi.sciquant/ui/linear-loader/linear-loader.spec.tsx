import React from "react";
import { render } from "@testing-library/react";
import { BasicLinearLoader } from "./linear-loader.composition";

describe("linear-loader", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicLinearLoader />);
  //   const rendered = getByText('hello from LinearLoader');
  //   expect(rendered).toBeTruthy();
  // });
});
