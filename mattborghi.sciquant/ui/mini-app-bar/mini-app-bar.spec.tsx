import React from "react";
import { render } from "@testing-library/react";
import { BasicMiniAppBar } from "./mini-app-bar.composition";

describe("mini-app-bar", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicMiniAppBar />);
  //   const rendered = getByText('hello from MiniAppBar');
  //   expect(rendered).toBeTruthy();
  // });
});
