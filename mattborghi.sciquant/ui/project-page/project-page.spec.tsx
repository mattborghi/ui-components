import React from "react";
import { render } from "@testing-library/react";
import { BasicProjectPage } from "./project-page.composition";

describe("project-page", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicProjectPage />);
  //   const rendered = getByText('hello from ProjectPage');
  //   expect(rendered).toBeTruthy();
  // });
});
