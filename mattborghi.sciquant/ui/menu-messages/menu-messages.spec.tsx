import React from "react";
import { render } from "@testing-library/react";
import { BasicMenuMessages } from "./menu-messages.composition";

describe("menu-messages", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicMenuMessages />);
  //   const rendered = getByText('hello from MenuMessages');
  //   expect(rendered).toBeTruthy();
  // });
});
