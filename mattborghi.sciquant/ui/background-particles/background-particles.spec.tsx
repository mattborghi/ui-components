import React from "react";
import { render } from "@testing-library/react";
import { BasicBackgroundParticles } from "./background-particles.composition";

describe("background-particles", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicBackgroundParticles />);
  //   const rendered = getByText('hello from BackgroundParticles');
  //   expect(rendered).toBeTruthy();
  // });
});
