import React from "react";
import { render } from "@testing-library/react";
import { BasicLeftAppbar } from "./left-appbar.composition";

describe("left-appbar", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicLeftAppbar />);
  //   const rendered = getByText('hello from LeftAppbar');
  //   expect(rendered).toBeTruthy();
  // });
});
