import React from "react";
import { render } from "@testing-library/react";
import { BasicVerticalStepper } from "./vertical-stepper.composition";

describe("vertical-stepper", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicVerticalStepper />);
  //   const rendered = getByText('hello from VerticalStepper');
  //   expect(rendered).toBeTruthy();
  // });
});
