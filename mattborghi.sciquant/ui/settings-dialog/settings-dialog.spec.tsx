import React from "react";
import { render } from "@testing-library/react";
import { BasicSettingsDialog } from "./settings-dialog.composition";

describe("settings-dialog", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicSettingsDialog />);
  //   const rendered = getByText('hello from SettingsDialog');
  //   expect(rendered).toBeTruthy();
  // });
});
