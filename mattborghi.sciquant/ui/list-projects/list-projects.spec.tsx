import React from "react";
import { render } from "@testing-library/react";
import { BasicListProjects } from "./list-projects.composition";

describe("list-projects", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicListProjects />);
  //   const rendered = getByText('hello from ListProjects');
  //   expect(rendered).toBeTruthy();
  // });
});
