import React from "react";
import { render } from "@testing-library/react";
import { BasicModelCreated } from "./model-created.composition";

describe("model-created", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicModelCreated />);
  //   const rendered = getByText('hello from ModelCreated');
  //   expect(rendered).toBeTruthy();
  // });
});
