import React from "react";
import { render } from "@testing-library/react";
import { BasicAccordionItem } from "./accordion-item.composition";

describe("accordion-item", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicAccordionItem />);
  //   const rendered = getByText('hello from AccordionItem');
  //   expect(rendered).toBeTruthy();
  // });
});
