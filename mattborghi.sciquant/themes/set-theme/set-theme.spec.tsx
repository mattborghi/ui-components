import React from "react";
import { render } from "@testing-library/react";
import { DarkSetTheme, LightSetTheme } from "./set-theme.composition";

describe("set-theme", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicSetTheme />);
  //   const rendered = getByText('hello from SetTheme');
  //   expect(rendered).toBeTruthy();
  // });
});
