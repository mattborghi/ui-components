import React from "react";
import { render } from "@testing-library/react";
import { BasicToggleTheme } from "./toggle-theme.composition";

describe("toggle-theme", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicToggleTheme />);
  //   const rendered = getByText('hello from ToggleTheme');
  //   expect(rendered).toBeTruthy();
  // });
});
