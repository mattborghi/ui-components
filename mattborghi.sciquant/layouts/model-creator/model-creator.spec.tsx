import React from "react";
import { render } from "@testing-library/react";
import { BasicModelCreator } from "./model-creator.composition";

describe("model-creator", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicModelCreator />);
  //   const rendered = getByText('hello from ModelCreator');
  //   expect(rendered).toBeTruthy();
  // });
});
