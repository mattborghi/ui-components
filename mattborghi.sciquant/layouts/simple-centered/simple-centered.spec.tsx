import React from "react";
import { render } from "@testing-library/react";
import { BasicSimpleCentered } from "./simple-centered.composition";

describe("simple-centered", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicSimpleCentered />);
  //   const rendered = getByText('hello from SimpleCentered');
  //   expect(rendered).toBeTruthy();
  // });
});
