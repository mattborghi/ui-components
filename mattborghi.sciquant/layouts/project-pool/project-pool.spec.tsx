import React from "react";
import { render } from "@testing-library/react";
import { BasicProjectPool } from "./project-pool.composition";

describe("project-pool", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicProjectPool />);
  //   const rendered = getByText('hello from ProjectPool');
  //   expect(rendered).toBeTruthy();
  // });
});
