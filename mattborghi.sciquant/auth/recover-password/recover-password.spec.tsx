import React from "react";
import { render } from "@testing-library/react";
import { BasicRecoverPassword } from "./recover-password.composition";

describe("recover-password", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicRecoverPassword />);
  //   const rendered = getByText('hello from RecoverPassword');
  //   expect(rendered).toBeTruthy();
  // });
});
