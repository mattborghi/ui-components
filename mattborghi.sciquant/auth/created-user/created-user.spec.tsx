import React from "react";
import { render } from "@testing-library/react";
import { BasicCreatedUser } from "./created-user.composition";

describe("created-user", () => {
  it("dummy", () => {
    true;
  });
  // it('should render with the correct text', () => {
  //   const { getByText } = render(<BasicCreatedUser />);
  //   const rendered = getByText('hello from CreatedUser');
  //   expect(rendered).toBeTruthy();
  // });
});
